# syntax
# map(function, iterable)
import math

l = [1, 2, 3, 4]

# squared = list(map(lambda a: a ** 2, l))
# print(l)
# print(squared)

# using separate methods

# def cube(x: int) -> int:
#     return math.floor(math.pow(x, 3))
#
#
# cubed = list(map(cube, l))
# print(cubed)

# displayed = list(map(lambda x: print(x), l)) returns List of None after executing statements
# print(displayed)
